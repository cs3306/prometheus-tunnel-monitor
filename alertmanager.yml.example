global:
  resolve_timeout: 5m

route:
  receiver: tgbot
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  - name: tgbot
    webhook_configs:
      - url: 'http://tgbot:8000/alertmanager'
        send_resolved: true
